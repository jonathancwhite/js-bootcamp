let studentScore = 100
let maxScore = 100
let decimalHundred = (studentScore / maxScore) * 100

// Formatting to allow % to be added
let returnFormatted = decimalHundred + '%'

console.log(returnFormatted)